import 'package:flutter/material.dart';

const programName = 'Simple POS';

const userDatabase = 'root';

const databaseName = 'simple_pos';

const bgColor = Color(0xFF1E2126);

const bgColor2 = Color(0xFF16191C);

const primaryColor = Color(0xFFD6A578);

const secondaryColor = Color(0xFFFBEBD1);
